# Nixpacks config for chat-server directory
# Set NO_CACHE=1 in Railway environment variables to disable build caching

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
# Use npm install instead of npm ci to handle workspace/lock file sync issues
cmds = ["npm install --legacy-peer-deps"]

[start]
cmd = "node server.js"
