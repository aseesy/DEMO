# Nixpacks config for chat-server directory
# Used when Railway Root Directory is set to "chat-server"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[start]
cmd = "node server.js"
