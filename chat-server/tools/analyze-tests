#!/bin/bash
# Wrapper script for analyze_tests.py

cd "$(dirname "$0")/.." || exit 1

# Activate venv if it exists
if [ -d ".venv" ]; then
  source .venv/bin/activate
fi

# Run the Python script
python3 tools/analyze_tests.py "$@"
