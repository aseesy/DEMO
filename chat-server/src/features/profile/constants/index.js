/**
 * Profile Constants - Profile field definitions and privacy settings
 * @module features/profile/constants
 */

const {
  SENSITIVE_FIELDS,
  PROFILE_SECTIONS,
  VALID_FIELD_VALUES,
  DEFAULT_PRIVACY_SETTINGS,
} = require('./profileConstants');

module.exports = {
  SENSITIVE_FIELDS,
  PROFILE_SECTIONS,
  VALID_FIELD_VALUES,
  DEFAULT_PRIVACY_SETTINGS,
};
