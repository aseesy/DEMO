/**
 * Invitation Services Index
 */

const { invitationService, InvitationService } = require('./invitationService');
const { invitationEmailService, InvitationEmailService } = require('./invitationEmailService');

module.exports = {
  invitationService,
  InvitationService,
  invitationEmailService,
  InvitationEmailService,
};
