# Add to existing .gitignore if it exists, or create new
# This ensures we don't commit generated files

# Naming convention validator output (if we add logging)
*.naming-validation.log
node_modules
screenshots/*.webm
.vercel
