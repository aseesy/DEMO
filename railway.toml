# Railway Configuration
# Deploy from chat-server directory directly

[build]
builder = "nixpacks"
buildCommand = "npm ci"

[deploy]
startCommand = "cd /app && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

