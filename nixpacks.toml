[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["cd chat-server && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd chat-server && echo 'Build complete'"]

[start]
cmd = "cd chat-server && node server.js"
