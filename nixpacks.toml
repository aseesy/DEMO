# Root nixpacks.toml - NOT USED by Railway
# Railway uses chat-server/nixpacks.toml because root_dir=chat-server
# This file is kept for reference only

# See chat-server/nixpacks.toml for the actual deployment config
