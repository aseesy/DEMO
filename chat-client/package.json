{
  "name": "chat-client",
  "version": "1.0.0",
  "description": "Multi-user chat room frontend",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html --no-browser --no-css-inject",
    "start": "npx http-server -p 3000"
  },
  "keywords": ["chat", "react", "websocket"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "socket.io-client": "^4.6.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
